/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["images.oyoroomscdn.com", "img.icons8.com", "www.google.com"],
	},
};

module.exports = nextConfig;
